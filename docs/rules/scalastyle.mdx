---
id: scalastyle
title: Scalastyle
---

import Rules from "./Rules";
import data from "./sonar-scala-metadata";

export const rules = data.repositories["sonar-scala-scalastyle"].rules.filter(
  rule => !rule.template
);

export const templates = data.repositories[
  "sonar-scala-scalastyle"
].rules.filter(rule => rule.template);

## Rules

This is a collection of all the Scalastyle rules created by sonar-scala in the
Scalastyle rules repository.

<Rules rules={rules} />

## Templates

<Rules rules={templates} />
